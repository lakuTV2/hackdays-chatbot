//Integrates and configures Flyway to connect to H2
//flyway {
//      url='jdbc:postgresql://localhost:5432/quartzds'
//      user='campexqrtz'
//      password = 'Sy7K3yqbry2k3xnW'
//      schemas = ['qrtz_blob_triggers']
//      locations=["filesystem:${new File('.').canonicalPath}/src/main/environments/common/db/sql","filesystem:${new File('.').canonicalPath}/src/main/environments/$environment/db/sql"]
//}

// The following lines are used for updating; if removed you will be repsonsible for merging/updating file.
// Template=TV2default
// TemplateVersion=1.0.0
